# Antigravity CLI driver

The implemented child component and its acceptance limits are documented in
[the Agy component](../../src/driver/agy/README.md). The shared
[driver contract](../../src/driver/README.md) defines the boundary used by all
harnesses. Production managed launches still require host profiles, login setup
and the gateway activation checks.
