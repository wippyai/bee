version: '1.0'
namespace: bee.desktop.admission.probe
entries:
- name: policy
  kind: security.policy
  policy:
    actions: [process.spawn, process.spawn.monitored, process.host, process.send, process.cancel, process.terminate, process.monitor, process.registry.lookup, process.context, process.security, security.scope.create, security.policy.get]
    resources: '*'
    effect: allow
- name: names
  kind: security.policy
  policy:
    actions: [process.registry.register, process.registry.unregister, process.registry.register.eventual, process.registry.unregister.eventual]
    resources: [bee.hive.supervisor, 'bee.hive.supervisor/*', bee.desktop.admission.probe.release]
    effect: allow
- name: inspect
  kind: security.policy
  policy:
    actions: [system.read]
    resources: [node]
    effect: allow
- name: service_grants
  kind: security.policy
  policy:
    actions: [registry.get]
    resources: [bee.hive.service:supervisor_service]
    effect: allow
- name: client_policy
  kind: security.policy
  policy:
    actions: [process.send, process.spawn, process.spawn.monitored, process.host, process.registry.lookup, tty.attach, tty.read, tty.write, tty.resize, tty.viewport]
    resources: '*'
    effect: allow
- name: coordinator
  kind: process.lua
  source: file://coordinator.lua
  method: main
  modules: [process, security, time, channel, io, system]
  imports: {types: bee.hive:types}
  meta:
    command:
      name: hive-desktop-admission-probe
      security:
        actor: {id: bee.desktop.probe}
        policies: [bee.desktop.admission.probe:policy, bee.desktop.admission.probe:inspect, bee.desktop.admission.probe:service_grants, bee.desktop.admission.probe:names]
- name: client
  kind: process.lua
  source: file://client.lua
  method: main
  modules: [process, time, channel, uuid]
  imports:
    types: bee.hive:types
    protocol: bee.hive.desktop:protocol
    contract: bee.protocol:application
    display: bee.hive.desktop:display
