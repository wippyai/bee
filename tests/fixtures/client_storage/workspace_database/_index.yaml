version: '1.0'
namespace: bee.workspace.db
entries:
- name: left
  kind: db.sql.sqlite
  file: ${env:bee.env:client_db_path}.workspace-left
- name: right
  kind: db.sql.sqlite
  file: ${env:bee.env:client_db_path}.workspace-right
