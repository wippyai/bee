version: '1.0'
namespace: bee.driver.muse
entries:
- name: definition
  kind: ns.definition
  module: driver-muse
  readme: file://README.md
  meta:
    title: Bee Muse driver
    comment: Optional harness binding; host activation and launch policies authorize its use
- name: executable
  kind: env.variable
  storage: bee.harness.host:environment
  variable: muse
  default: ''
  readonly: true
- name: environment
  kind: env.storage.os
  lifecycle:
    auto_start: true
- name: config_home
  kind: env.variable
  storage: bee.driver.muse:environment
  variable: XDG_CONFIG_HOME
  default: ''
  readonly: true
- name: protocol
  kind: library.lua
  source: file://protocol.lua
  modules: [json]
  imports:
    events: bee.driver.kit:events
    types: bee.driver:types
- name: configuration
  kind: library.lua
  source: file://configuration.lua
  modules: [hash]
  imports:
    bounds: bee.threads.records:bounds
    canonical: bee.threads.records:canonical
    configure_protocol: bee.driver:configuration
    quote: bee.driver.kit:quote
- name: launch
  kind: library.lua
  source: file://launch.lua
  imports:
    bounds: bee.threads.records:bounds
    types: bee.driver:types
- name: prepare
  kind: function.lua
  source: file://prepare_method.lua
  method: handle
  imports:
    launch: bee.driver.muse:launch
    types: bee.driver:types
- name: configure
  kind: function.lua
  source: file://configure_method.lua
  method: handle
  imports:
    configuration: bee.driver.muse:configuration
    configure_protocol: bee.driver:configuration
- name: dispatch
  kind: function.lua
  source: file://dispatch_method.lua
  method: handle
  imports:
    launch: bee.driver.muse:launch
    types: bee.driver:types
- name: normalize
  kind: function.lua
  source: file://normalize_method.lua
  method: handle
  imports:
    protocol: bee.driver.muse:protocol
    bounds: bee.threads.records:bounds
- name: binding
  kind: contract.binding
  contracts:
  - contract: bee.driver:driver
    methods:
      prepare: bee.driver.muse:prepare
      dispatch: bee.driver.muse:dispatch
      normalize: bee.driver.muse:normalize
      configure: bee.driver.muse:configure
  meta:
    type: harness.driver
    driver_id: muse
    profiles_ref: bee.driver.muse:profiles
- name: profiles
  kind: registry.entry
  meta:
    type: harness.profile
    driver_ref: bee.driver.muse:binding
  data:
    driver:
      schema_revision: bee.driver@1
      kind: harness
      title: Muse CLI
      implementation_version: 1.3.0
      default_profile: batch
      profiles:
      - id: batch
        mode: batch
        hooks:
          transports:
          - command
          events:
          - SessionStart
          - UserPromptSubmit
          - PreToolUse
          - PostToolUse
          - PermissionRequest
          - Stop
          - SessionEnd
          adapter_ref: bee.driver.muse:protocol
        inbound:
        - next_turn
        protocol: stream-json
        protocol_revision: msp-exec-1
        answer_path:
          strategy: accumulate
          adapter_ref: bee.driver.muse:protocol
        resume:
          strategy: per-process
          portable: false
        isolation_env:
          variables:
          - HOME
          - XDG_CONFIG_HOME
          private_home: true
        exit_codes_trustworthy: false
        input_ready:
          strategy: protocol
          adapter_ref: bee.driver.muse:protocol
          timeout_ms: 15000
        interrupt:
          methods:
          - signal_group
        mcp:
          client_transports:
          - streamable_http
          initialize_timeout_ms: 30000
        sandbox:
          providers:
          - muse-sandbox
          required_placement_features: []
      - id: window
        mode: window
        protocol: pty
        protocol_revision: native-window-1
        answer_path: {strategy: none}
        resume: {strategy: none, portable: false}
        isolation_env:
          variables: [HOME, XDG_CONFIG_HOME]
          private_home: false
        exit_codes_trustworthy: false
        input_ready: {strategy: none}
        permission_exchange: {mode: none}
- name: default_window
  kind: registry.entry
  meta:
    type: bee.launch_definition
    comment: Component-owned Muse window declaration; the host separately selects its policy, activation, executable and permissions
  data:
    schema_revision: bee.launch-definition@1
    launch_id: muse-window
    title: Muse CLI
    command_names: [muse]
    binding_ref: bee.driver.muse:binding
    profile_id: window
    policy_ref: bee:launch_policy_muse_window
    default_mode: window
    allowed_overrides: []
    workdir_policy: {kind: declared_resource, resource_ref: project}
    thread_policy: {kind: new}
    session_resource: session
    credentials: []
    presentation: {start_menu: true, fullscreen: true, reuse: never}
- name: research_batch
  kind: registry.entry
  meta:
    type: bee.launch_definition
    comment: Component-owned bounded Muse batch route for a host-selected coordinator and caller-owned shared thread
  data:
    schema_revision: bee.launch-definition@1
    launch_id: muse-research-batch
    title: Muse researcher
    command_names: []
    binding_ref: bee.driver.muse:binding
    profile_id: batch
    policy_ref: bee:launch_policy_muse_batch
    default_mode: batch
    allowed_overrides: [thread]
    workdir_policy: {kind: declared_resource, resource_ref: project}
    thread_policy: {kind: caller}
    session_resource: session
    credentials: [muse_login]
    presentation: {start_menu: false, fullscreen: false, reuse: never}
- name: credential_format
  kind: registry.entry
  meta:
    type: bee.credential_format
  data:
    schema_revision: bee.credential-format@1
    file:
      path: .config/muse/auth.json
      content_format: opaque
      initialize: []
