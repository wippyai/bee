version: '1.0'
namespace: bee.resources
entries:
- name: definition
  kind: ns.definition
  module: resources
  readme: file://README.md
  meta:
    title: Bee resources
    comment: Owner-local resource authority; associations under a host ceiling, grants bound to subject, audience, revision, digest, scope and expiry
- name: dependency_persist
  kind: ns.dependency
  component: bee/persist
  version: '*'
- name: dependency_threads
  kind: ns.dependency
  component: bee/threads
  version: '*'
- name: target_db
  kind: ns.requirement
  meta:
    comment: SQL resource for associations and grants
  default: bee.resources:db
  targets:
  - entry: bee.resources:database_ref
    path: .resource_ref
- name: target_roots
  kind: ns.requirement
  meta:
    comment: The host's admitted root list, the ceiling every association stays under
  targets:
  - entry: bee.resources:roots_ref
    path: .resource_ref
- name: database_ref
  kind: registry.entry
  meta:
    type: bee.resource_ref
    comment: The linked SQL resource; target_db fills resource_ref
- name: roots_ref
  kind: registry.entry
  meta:
    type: bee.resource_ref
    comment: The linked host roots entry; target_roots fills resource_ref
- name: environment
  kind: env.storage.os
  lifecycle:
    auto_start: true
- name: db_path
  kind: env.variable
  storage: bee.resources:environment
  variable: BEE_RESOURCES_DB
  default: .wippy/resources.db
  readonly: true
- name: db
  kind: db.sql.sqlite
  file: ${env:bee.resources:db_path}
  lifecycle:
    auto_start: true
- name: contract
  kind: contract.definition
  meta:
    comment: Associations and grants for one node's workspaces; resolve is for admitted placements only
  methods:
  - name: associate
    description: Bind a workspace name to an admitted root under the host ceiling; optional expected_revision supports atomic create or CAS replacement
  - name: grant
    description: Issue a grant to the authenticated subject for an audience, pinned to the association revision and root digest; a caller admitted to grant_thread may name a thread-bound subject with its thread instead
  - name: revoke
    description: End one grant
  - name: revoke_all
    description: Advance the workspace authorization epoch so every earlier grant stops resolving, reporting the fenced live attempts for eager stops
  - name: resolve
    description: Re-check a grant for the placement that admitted the subject and audience, and return the exact resource scope
  - name: list
    description: A manager's view of associations and live grants
  - name: describe
    description: One workspace's associations as a titled list, for a caller that may read the workspace; the shape of a workspace catalog extension
  - name: search
    description: One workspace's associations whose name starts with text, for a caller that may read the workspace
- name: local
  kind: contract.binding
  contracts:
  - contract: bee.resources:contract
    methods:
      associate: bee.resources.binding:associate
      grant: bee.resources.binding:grant
      revoke: bee.resources.binding:revoke
      revoke_all: bee.resources.binding:revoke_all
      resolve: bee.resources.binding:resolve
      list: bee.resources.binding:list
      describe: bee.resources.binding:describe
      search: bee.resources.binding:search
- name: resources
  kind: library.lua
  source: file://resources.lua
  modules: [registry, env, system]
