# Build sequence

Status: the consolidated order of work from the six design rounds of
2026-09-08, in [thread records](THREAD_RECORDS.md),
[component layout](COMPONENT_LAYOUT.md), [registry extension](REGISTRY_EXTENSION.md),
[launch routing](LAUNCH_ROUTING.md) and
[placement and subscriptions](PLACEMENT_AND_SUBSCRIPTIONS.md). Each step names
what it touches, what it depends on and the proof that ends it. A step is
done when its proof passes on a populated store and after a restart, not when
its code exists. The cluster work proceeds in parallel; nothing below waits on
it, because owner epochs, resumable reads and uncertain outcomes are explicit
from step 2.

Current managed-agent checkpoint (September 12): four separately packaged harness
components have default window profiles and first-use resource setup. Claude and
Codex additionally have optional machine-login delivery with source binding,
private retained homes, and present/absent executable acceptance for each provider.
The global binary includes these changes and scoped MCP/hook activation, verified
through fixture harness executables. Authenticated provider orchestration, Docker
and successful cold conversation recovery remain open; the lower-level step proofs
below do not imply those workflows are complete. Source also supports a
host-selected instruction function with inherited actor/context and frozen
launch delivery; the picker does not yet edit that policy field. See
[Agent integration](handoffs/AGENT_INTEGRATION.md) and
[current global build](handoffs/GLOBAL_BUILD.md).

| # | Step | Touches | Depends on | Proof |
|---|---|---|---|---|
| 1 | Module roots and dependency interfaces. Done 2026-09-08: `bee.threads:definition`, `target_db` linked into `bee.threads:database_ref`, `make threads-module` | `bee.threads` root (`ns.definition`, requirement targets), `tests/modules/threads` standalone app | nothing | the thread closure loads in isolation with no desktop, Kickside, model or MCP; an unlinked reference is refused at boot (the linker itself does not reject dangling targets) |
| 2 | Records and additive thread authority. Done 2026-09-08: `bee.threads.records`, `.service`, `.persist`, migrations 2 and 3, contracts `authority` and `lifecycle`, Wippy suites under `tests/lua/threads` | `bee.threads.records`, `.service`, `.persist`, migrations `thread_authority`, `work_lifecycle` | 1 | legacy journal callers unchanged; populated-store upgrade; authenticated rich records; conflicting retries fail without changing history; one terminal receipt per identity |
| 3 | Delivery, waits and projection. Done 2026-09-08: `bee.threads.delivery` with the waiter service, `bee.threads.projection`, `bee.threads:owner`, migrations 4 and 5, Wippy suites | `bee.threads.delivery` (+ `waiter` service), `.projection`, migrations `delivery`, `projection` | 2 | correlated fan-out to two recipients; 60 s and short slices; lost acknowledgment; drain releases waiters; recap rebuilt from records and stale checkpoints rejected |
| 4 | Approvals owner and outbox. Built 2026-09-09: `bee.approvals` (requests bound to a canonical proposal digest under host approver policies, compare-and-set decisions, owner expiry, withdrawal, consumption bound to one effect key, bounded inbox, durable outbox with a worker) delivering through the narrow ingress `bee.threads.approvals:append` for the `approval.request` and `approval.transition` families (thread migration 7); the permission exchange is specified in APPROVALS.md and built as the pure `bee.harness.permission:adapter` and `acceptance` with profile pinning, catalog eligibility and the live fixture acceptance runner; the carrier runs the exchange under the fixture policy with the intent, approval, consumption, write and settlement crash proofs; the real Claude control protocol is captured (`control.jsonl`, 2026-09-09), described by `bee.driver.claude:permission_adapter`, pinned by both Claude profiles and proven with the executable at the adapter level (`claude_acceptance_test`: request, wait, allow once, deny, wrong correlation, silence) and through the carrier (`claude_control_test`: allow, deny, expiry); shipped launch policies still enable no exchange; the local approvals inbox application `bee.inbox:app` is built 2026-09-09 over the owner's inbox, read, decide and withdraw (see APPROVALS.md, presentation) | `bee.approvals` (service, persist, security, migrations) | 1 | two approvers race, one decision; owner enforces expiry; thread projection repairs after restart; boots with no thread available |
| 5 | Subscriptions and cross-node send. Contract and local proofs 2026-09-09: `docs/THREAD_SESSIONS.md`, `bee.threads.service:send`/`send_status` under the caller node and idempotency key with the sender's payload digest, `bee.threads.delivery:session` reconnect transitions; owner-authorized `close_subscription`/`forget_subscription` with a bounded retained-subscription store (2026-09-09); forwarding over the supervisor seam and two-runtime proofs not built, cross-node capabilities stay false | thread `read_after`, `subscribe`, `ack`, `unsubscribe`, `send` with owner-qualified refs | 3 | ordered at-least-once delivery with filter-bound cursors; `scanned_through` progress; `CURSOR_EXPIRED`; A sends to B's thread and a retry with the same idempotency key commits once |
| 6 | Workspace resources and credential broker. Resources built 2026-09-09: `bee.resources` (associations under the host ceiling, grants bound to the authenticated subject, audience, association revision, root digest, scope, expiry and authorization epoch; `resolve` for placements only), placement `resource_mode` selected by the host with recheck at start and enforcement at reconcile. Credentials built 2026-09-09: `bee.credentials` (definitions from host-admitted `env.variable` sources, projections bound to subject, audience, attempt, profile, binding and policy digests, environment projections only for `ANTHROPIC_API_KEY` and `OPENAI_API_KEY`, bytes returned once to the placement runner), placement supervision sweeper bounding revocation enforcement; provider file formats are separately reviewed later; symlink-escape containment, restart persistence and no-secret export proven 2026-09-09 (`tests/resources.py`) | resource-resolution contract, workspace associations, broker over host secrets | existing workspace and storage | a launch names resource refs only; admission intersects grants; `RESOURCE_NOT_LOCAL` for another node's resource; no credential bytes in argv, records or profiles |
| 7 | Driver contract, kit, fixtures. Codex API-key authentication-path selection proven 2026-09-09 through the placement runner against the pinned executable (`tests/lua/harness/codex_runner_test.lua`, runs when `BEE_CODEX_BIN` names it and the runtime can close stdin): the host launch policy names a `bee.codex_provider` entry, `bee.driver.codex:configuration` renders the reviewed provider fields into the private home's `.codex/config.toml` with exclusive creation, the credential broker projects the sentinel `OPENAI_API_KEY`, the launch writes its brief and closes stdin once (`stdin_eof`, runtime PR 698), and a controlled loopback endpoint records `Authorization: Bearer <sentinel>`; a changed provider refuses resume before start, a policy without a provider refuses the plan, a post-EOF write is refused on record, and a runtime without stdin closure refuses placement. The endpoint's 400 proves neither provider acceptance nor a completed turn; `launch.CODEX_AUTHENTICATION` stays `unproven` until the pinned build carries the runtime capabilities. Claude API-key authentication-path selection proven 2026-09-09 the same way, settings-free and environment-only (`tests/lua/harness/claude_runner_test.lua`, runs when `BEE_CLAUDE_BIN` names the version-recorded executable): the launch policy selects the endpoint through its `environment` (`ANTHROPIC_BASE_URL`), the broker projects the sentinel `ANTHROPIC_API_KEY`, the runner's private home carries no login state, the endpoint records `x-api-key: <sentinel>` at `/v1/messages`, and no stream, record, evidence or settlement carries the sentinel; `launch.CLAUDE_AUTHENTICATION` stays `unproven` on the same condition. The managed-launch state, the runtime patch order, production requirements against fixture exceptions and the acceptance commands are in `docs/handoffs/MANAGED_LAUNCH_ACCEPTANCE.md` (`make managed-launch-check`). Done 2026-09-08 for stream-json: `bee.driver`, `.kit`, `.transport` (stream_json), `bee.driver.claude`, `bee.driver.codex`, captured fixtures for `stream-json-2` and `exec-json-1`; private managed PTY consumer has broker input/resize, retention and cancellation acceptance (see `docs/handoffs/NATIVE_WINDOW.md`); catalog matches PTY to window mode, while production interactive profiles and public launch remain pending | `bee.driver`, `.kit`, `.transport.stream_json`, `.transport.pty`, `tests/fixtures/drivers/<harness>/<protocol_revision>/` | 2 | invalid profiles and incompatible protocol revisions rejected; captured Claude and Codex streams normalize to expected records; partial frames and disconnects handled |
| 8 | Native placement. Built 2026-09-09: `bee.placement` contract and values, `bee.placement.native` with a runner process per attempt, receipts through `bee.persist`, derived homes, measured cleanup capability; group cleanup proven against runtime PR 694 and fails closed at `prepare` on the pinned runtime | `bee.placement` contract, `bee.placement.native` with process groups and private homes | 6 | child and grandchild cleanup; environment isolation; uncertain launch recovery; runtime process-group support is a blocker for this claim |
| 9 | Launch definitions, gateway, carriers, Claude and Codex locally. Catalog done 2026-09-08: `bee.harness.catalog` classifies `harness.driver` bindings in one registry generation, activation from `bee:harness_activation`; `bee.threads:capabilities` reports limits and versions. Carrier contract in threads done 2026-09-09 (`prepare_attempt`, `bee.threads:carrier`, migration 6, [CARRIER.md](CARRIER.md)). Carrier phase 1 done 2026-09-09: `bee.harness.carrier` runs the shipped Claude binding against the Claude protocol fixture through native placement, settles from the terminal envelope, recovers from every agreed crash point. Launch admission done 2026-09-09: `bee.harness.launch` decodes `bee.launch_definition` entries, resolves a measured plan, admits for the authenticated requester with attempt-bound grants and projections keyed on the request id, and starts or resumes the carrier; `bee:launch_policy_managed` exists and stays inert. Public launch wiring, the gateway and managed-harness enablement remain | `bee.launch_definition` entries, `bee.harness.catalog`, `.carrier`, `bee.gateway` with per-attempt credentials, `bee.driver.claude`, `bee.driver.codex`; Terminal `commands` become facades | 3, 4, 7, 8 | CLI and Start resolve the same definition digest; launch creates thread, action and one attempt; readiness gates the brief; a two-turn consultation settles with the right reply; F12 and a client restart neither duplicate the child nor end the action; explicit termination records the true outcome |
| 10 | Status surface and desktop apps | projection feeds for tab, title, recap, Hive Manager; `bee.thread_view`, `bee.timeline`, `bee.inbox` | 3, 4, 9 | status changes only from committed records; disconnected window shows last confirmed state; Timeline resumes from its cursor; Inbox distinguishes an unreachable owner from an empty inbox |
| 11 | Docker session placement | `bee.placement.docker` on `userspace.docker:narrow`, `bee.placement_profile` entries, workspace launch document | 6, 8, 9 | hardened container per attempt with the project mounted from a resource ref; one process per turn; creation intent recorded before dispatch; reconciliation by attempt labels; terminal evidence survives cleanup |
| 12 | Native runner and dataflow batch | `bee.agent.native` | 9 | one admitted runner turn and a two-node dataflow settle with the same request, delivery and receipt queries, no PID, PTY or MCP |
| 13 | Freeze and extract | schema revisions frozen; accepted closures moved to `modules/<package>/` with `test/`; `src/` mounts them by `ns.dependency` | 2 to 12 | host assembly and independently mounted package behave identically; no duplicate ids; fixture manifests and restart evidence published |
| 14 | Remote placement and attachment | destination admission, resource mapping, owner control operations, remote viewport binding | 5, 9, 10, cluster transport | child, carrier, gateway and PTY run on the destination; Mac sleep leaves work running; reconnect shows current frames and missed records; remote approval decided once; destination restart, lost launch ack and revoked attachment are distinguished |
| 15 | Docker windows, ACP, pi RPC | interactive executor route, `bee.driver.transport.acp`, `.rpc`, further vendor bindings | 11, 13 | interactive route proves identical admission and one container; new transports reuse existing authority contracts and record families |
| 16 | Registry extension lane and `bee:publish` | catalog generations, admission of overlays and Hub modules, `bee:publish` stage, validate, activate, status, rollback | 13 | a Hub driver module installs and appears in the catalog with no code change; an agent publishes a routine extension autonomously and a protected one only through an inbox request bound to the candidate digest |

## Parallel lanes

- Steps 1 to 5 are one lane and touch only `bee.threads*` and `bee.approvals`.
- Step 6 runs alongside them on the workspace and storage side.
- Steps 7 and 8 can start once step 2 lands; they need no delivery to be tested against fixtures.
- Step 9 is the first point where the lanes meet, and the first adversarial consumer of everything before it.
- Step 11 needs `userspace/docker` installed as a dependency and a digest-pinned image with the driver runtime in it.
- Step 14 is the first step that needs the cluster transport; everything before it works on one bee. The transport itself, supervisors, exposure and sessions are [the Hive protocol lane](HIVE_PROTOCOL.md), built in parallel.

## What is not in the sequence

Aider. Pause for batch mode. SIGSTOP. Bind-mounting host binaries. Per-driver executor entries. Any claim of Hive support before step 14's proof.
