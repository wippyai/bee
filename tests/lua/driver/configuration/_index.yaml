version: '1.0'
namespace: bee.driver
entries:
- name: configuration_test
  kind: function.lua
  source: file://configuration_test.lua
  method: run
  meta: {type: test, suite: bee}
  modules: [hash]
  imports:
    test: wippy.test:test
    configuration: bee.driver:configuration
    claude: bee.driver.claude:configure
    codex: bee.driver.codex:configure
    grok: bee.driver.grok:configure
    agy: bee.driver.agy:configure
