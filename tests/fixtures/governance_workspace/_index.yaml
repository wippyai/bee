version: '1.0'
namespace: bee.gov.workspace.probe
entries:
- name: workers
  kind: process.host
  host: {workers: 2, max_processes: 8}
  lifecycle: {auto_start: true}
- name: sender
  kind: library.lua
  source: file://sender.lua
  imports: {transaction: bee.persist:transaction, version: bee.sync:version}
- name: owner_policy
  kind: security.policy
  policy:
    actions: [funcs.call, funcs.security, security.actor.create, security.scope.create, security.policy.get]
    resources: '*'
    effect: allow
- name: call_policy
  kind: security.policy
  policy:
    actions: [funcs.call]
    resources: [bee.gov.binding:overlay_call, bee.gov.binding:workspace_backend_call, bee.gov.workspace.probe:authority_probe]
    effect: allow
- name: caller_boundary
  kind: security.policy
  policy:
    actions: [db.get, security.scope.create, security.policy.get]
    resources: '*'
    effect: deny
- name: authority_probe
  kind: function.lua
  source: file://authority_probe.lua
  method: handle
  modules: [sql, security]
- name: read_policy
  kind: security.policy
  policy: {actions: [bee.gov.overlay.read], resources: 'demo', effect: allow}
- name: write_policy
  kind: security.policy
  policy: {actions: [bee.gov.overlay.write], resources: 'demo', effect: allow}
- name: main
  kind: process.lua
  source: file://main.lua
  method: main
  modules: [funcs, security, logger]
  imports: {bounds: bee.threads.records:bounds}
  security: {policies: [bee.gov.workspace.probe:owner_policy]}
  meta:
    command:
      name: governance-workspace-probe
      security:
        actor: {id: bee.gov.workspace.probe}
