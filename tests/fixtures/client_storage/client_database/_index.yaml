version: '1.0'
namespace: bee.client.db
entries:
- name: left
  kind: db.sql.sqlite
  file: ${env:bee.env:client_db_path}.left
- name: right
  kind: db.sql.sqlite
  file: ${env:bee.env:client_db_path}.right
- name: forbidden
  kind: db.sql.sqlite
  file: ${env:bee.env:client_db_path}.forbidden
