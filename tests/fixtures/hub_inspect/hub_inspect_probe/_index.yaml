version: '1.0'
namespace: bee.hub.inspect.probe
entries:
- name: workers
  kind: process.host
  host: {workers: 2, max_processes: 8}
  lifecycle: {auto_start: true}
- name: inspect_policy
  kind: security.policy
  policy:
    actions: [hub.versions.open, hub.versions.list, hub.modules.get, hub.modules.readme]
    resources: userspace/docker
    effect: allow
- name: catalog_policy
  kind: security.policy
  policy:
    actions: [hub.modules.search, hub.modules.list]
    resources: '*'
    effect: allow
- name: registry_read_policy
  kind: security.policy
  policy:
    actions: [registry.get]
    resources: '*'
    effect: allow
- name: main
  kind: process.lua
  source: file://main.lua
  method: main
  modules: [hub, logger, registry, base64]
  imports:
    inspect: bee.hub.binding:inspect
    catalog: bee.hub.binding:catalog
    inventory: bee.hub:inventory_reader
    preview: bee.hub.binding:preview
  security:
    policies: [bee.hub.inspect.probe:inspect_policy, bee.hub.inspect.probe:registry_read_policy, bee.hub.inspect.probe:catalog_policy]
  meta:
    command:
      name: hub-inspect-probe
      security:
        actor: {id: bee.hub.inspect.probe}
